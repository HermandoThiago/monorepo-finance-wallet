{
  "name": "wallet-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "bun run --watch src/index.ts",
    "start:build": "bun run build && bun run start"
  },
  "dependencies": {
    "elysia": "latest",
    "@mono-dev/drizzle": "workspace:*",
    "@mono-dev/types": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "tsx": "^4.21.0"
  },
  "module": "src/index.js"
}
